name: Bootstrap repository source

on:
  push:
    paths:
      - 'bootstrap/READY'

permissions:
  contents: write

jobs:
  unpack-source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Reconstruct source archive
        shell: bash
        run: |
          set -euo pipefail
          cat bootstrap/source.b64.part* | base64 --decode > /tmp/autolabor-c1-source.tar.gz
          tar -xzf /tmp/autolabor-c1-source.tar.gz
          rm -rf bootstrap
      - name: Commit unpacked source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            echo 'No source changes to commit.'
            exit 0
          fi
          git commit -m 'feat: import ROS2 C1 driver and Phase 1 RAW acceptance'
          git push origin HEAD:main
